<h1><%= @page_forum.title %></h1>

<div id="page_forum">
	<%= render partial: 'page_forum', locals: { page_forum: @page_forum } %>
</div>
<%= render partial: 'comments/form', locals: { page_forum: @page_forum } %>

